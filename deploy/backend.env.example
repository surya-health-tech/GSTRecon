# Copy to /opt/GSTRecon/backend/.env
JWT_SECRET_KEY=change-me-use-openssl-rand-hex-32
FRONTEND_APP_URL=http://206.189.140.76:8092
CORS_ORIGINS=http://206.189.140.76:8092

PLATFORM_ADMIN_EMAIL=admin@yourdomain.com
PLATFORM_ADMIN_PASSWORD=change-me-on-first-login-then-rotate
PLATFORM_ADMIN_NAME=Platform Admin

# Optional SMTP / OAuth — see backend/.env.example in repo root
